server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: account-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

springdoc:
  swagger-ui:
    path: /swagger-ui.html
